# Group / aggregate by country (|#). `count` is always emitted; add
# sum/avg/min/max on a numeric column. The group is a materialization boundary
# (results are emitted once the input stream ends).
Population:
    open examples/users.csv
    |# country sum:age avg:age max:age
;
